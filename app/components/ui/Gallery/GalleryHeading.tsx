'use client';

export default function GalleryHeading() {
  return (
    <h2
      className="text-[30px] md:text-[46px] font-bold "
      style={{
        fontFamily: "'Press Start 2P', cursive",
        color: '#FFFFFF',
        textShadow: '3px 3px 0px rgba(0, 0, 0, 0.2)',
        letterSpacing: '2px',
      }}
    >
      GALLERY
    </h2>
  );
}
